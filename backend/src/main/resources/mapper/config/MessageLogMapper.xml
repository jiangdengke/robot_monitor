<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.robotmonitor.config.mapper.MessageLogMapper">

    <resultMap type="MessageLog" id="MessageLogResult">
        <result property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="content"    column="content"    />
        <result property="status"    column="status"    />
        <result property="source"    column="source"    />
        <result property="roomCode"    column="room_code"    />
        <result property="createTime"    column="create_time"    />
        <result property="processor"    column="processor"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectMessageLogVo">
        select id, title, content, status, source, room_code, create_time, processor, update_time from message_log
    </sql>

    <select id="selectMessageLogList" parameterType="MessageLog" resultMap="MessageLogResult">
        <include refid="selectMessageLogVo"/>
        <where>
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="source != null  and source != ''"> and source = #{source}</if>
            <if test="roomCode != null  and roomCode != ''"> and room_code = #{roomCode}</if>
            <if test="processor != null  and processor != ''"> and processor = #{processor}</if>
        </where>
    </select>

    <select id="selectMessageLogById" parameterType="Long" resultMap="MessageLogResult">
        <include refid="selectMessageLogVo"/>
        where id = #{id}
    </select>

    <insert id="insertMessageLog" parameterType="MessageLog" useGeneratedKeys="true" keyProperty="id">
        insert into message_log
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">title,</if>
            <if test="content != null">content,</if>
            <if test="status != null">status,</if>
            <if test="source != null">source,</if>
            <if test="roomCode != null">room_code,</if>
            <if test="createTime != null">create_time,</if>
            <if test="processor != null">processor,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">#{title},</if>
            <if test="content != null">#{content},</if>
            <if test="status != null">#{status},</if>
            <if test="source != null">#{source},</if>
            <if test="roomCode != null">#{roomCode},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="processor != null">#{processor},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateMessageLog" parameterType="MessageLog">
        update message_log
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="source != null">source = #{source},</if>
            <if test="roomCode != null">room_code = #{roomCode},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="processor != null">processor = #{processor},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMessageLogById" parameterType="Long">
        delete from message_log where id = #{id}
    </delete>

    <delete id="deleteMessageLogByIds" parameterType="String">
        delete from message_log where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
